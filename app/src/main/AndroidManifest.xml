<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MR"
        tools:targetApi="31">

        <activity android:name=".Billing.UnitsActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".CurrentStatus.InventoryActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".Common.FindActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".Common.MainActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".CurrentStatus.ARActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".CurrentStatus.APActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".CurrentStatus.AIActivity"
            android:screenOrientation="portrait"/>
        <activity android:name=".Registration.RegistrationActivity"
            android:screenOrientation="portrait"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>